{
  "name": "grimoire-workspaces",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "prettier": "prettier . --write"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "yarn@4.15.0",
  "devDependencies": {
    "prettier": "3.8.3"
  }
}
